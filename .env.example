# Local backend
VITE_API_BASE_URL=http://localhost:8000

# Production (Render) — use HTTPS, no trailing slash; WebSocket becomes wss:// automatically
# VITE_API_BASE_URL=https://mathquiz-be.onrender.com
